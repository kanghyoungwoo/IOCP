[← README로 돌아가기](../README.md)

# 부하 테스트 리포트

## 테스트 환경

| 구분 | 사양 |
| --- | --- |
| **Server** | AWS EC2 c6i.4xlarge (16 vCPU, 32GB RAM) × 1대, Windows Server 2022 |
| **Network** | 동일 리전, 동일 클러스터 배치 그룹 (Cluster Placement Group) |
| **테스트 모드** | `TestMode=true` — Redis/MySQL I/O 우회, 순수 엔진 성능 측정 |

> 시나리오별 클라이언트 사양이 다릅니다. 수용량 테스트·Scenario B는 **c6i.xlarge (4 vCPU)**, Scenario A는 **c6i.large (2 vCPU)** 클라이언트를 사용했습니다.

---

## 10,000명 수용량 테스트 및 Zero-Leak 검증

c6i.xlarge 클라이언트 4대(각 2,500봇)로 10,000명의 더미 클라이언트가 실제 유저와 동일한 시나리오(로그인 → 방 입장 → 채팅 → 방 퇴장 → 재입장)를 3~5초 간격으로 반복하는 연속 부하 테스트를 수행했습니다. 결과는 **100방 × 100명** 구성 기준입니다.

### 1. 클라이언트 부하 테스트 지표

| 측정 항목 | 결과 수치 | 상태 요약 |
| --- | --- | --- |
| **최대 동시 접속 (Peak Conn)** | **10,000 명** | 목표 수용량 달성 |
| **연결 실패 / 끊김** | **0 건 / 10,000** | 세션 유실 0 |
| **평균 지연 시간** | **17.55 ms** | 실시간 통신 수준 유지 |
| **p99 지연 시간** | **~17ms** (최대 스파이크 744ms) | 초당 p99 측정 기준 — 전체 구간 중 99%의 구간에서 p99 17ms 이하. 최대 스파이크는 순간 burst에 의한 이상치 |
| **수신 처리량** | **134K pkts/s** | CPU 33%, 헤드룸 충분 |
| **이상 현상** | **없음** | 무중단 서비스 검증 완료 |

### 2. 서버 안정성 검증

| 서버 내부 지표 | 측정 값 | 의미 |
| --- | --- | --- |
| **메모리 점유** | **394 MB 고정** | 장기 운용 중 변동 없음 (누수 0) |
| **SendPool Alloc Fail** | **0 회** | 전 테스트 구간 누적 |
| **JobPool Alloc Fail** | **0 회** | Pool 고갈 없이 안정 순환 |
| **연결 실패** | **0 건 / 10,000** | 전 테스트 구간 세션 유실 없음 |

### 3. 트러블슈팅: OS 레벨 교차 검증을 통한 Zero-Allocation 증명

서버 내부 지표(Pool Free Count = 전량 반환)로 객체 누수가 없음을 확인한 뒤, AWS CloudWatch와 VS 프로파일러를 통한 OS 레벨 메모리 모니터링으로 교차 검증했습니다.

#### Phase 1: 실제 메모리 누수 발견 및 수정

- **현상**: 최초 10,000명 테스트에서 서버 메모리가 **99%(6,680MB)** 를 점유하며 **0.4MB/s** 씩 지속 증가
- **원인 분석**:
    1. **SendBuffer Pool 과잉 할당**: 봇당 400개씩 총 4,000,000개 할당 → 약 4.1GB 낭비
    2. **세션 버퍼 비대화**: `MAX_PACKET_DATA_BUFFER_SIZE`가 65,536으로 과도하게 설정
    3. **세션 종료 시 리소스 미반환**: `Closed()`에서 `Clear()` 미호출로 Send큐 잔존
- **해결**:
    1. Pool 크기를 봇당 **400 → 10**으로 최적화
    2. 버퍼 크기를 **65,536 → 8,192**로 조정
    3. `Closed()` 호출 시 `Clear()`로 세션 리소스 즉시 반환
- **결과**: 0.4MB/s 누수 해소, 16-core 환경 재측정에서 메모리 **394MB 고정**으로 안정화

#### Phase 2: OS 레벨 교차 검증을 통한 Zero-Leak 최종 증명

- **현상**: Phase 1 수정 후 메모리가 안정화되었으나, 10,000명 접속 해제 후에도 메모리가 떨어지지 않는 현상 발견
- **프로파일링**: VS 프로파일러 힙 스냅샷(Heap Snapshot)으로 분석. 10,000명 접속 전/후 스냅샷을 비교한 결과, 할당된 메모리의 증가량은 **+0 Bytes**
- **결론**: Lock-Free Object Pool이 해제된 세션/버퍼를 OS에 반납하지 않고 다음 접속을 위해 캐싱(Pre-allocation)하고 있었음. C++ CRT 힙 할당자의 정상 동작.
- **최종 성과**: 1만 명의 유저가 장시간 통신을 주고받는 환경에서도, 추가적인 동적 할당 없이 **394MB의 고정 메모리**로 무중단 서비스가 가능함을 확인

<img width="1479" height="215" alt="cloudwatch3시간" src="https://github.com/user-attachments/assets/ccd1e3c7-fb96-4dec-be92-123c762c58d2" />

---

## Scenario A: 채팅 빈도별 한계 탐색 — LockFree (16-core c6i.4xlarge)

채팅 빈도를 점진적으로 높여가며 서버의 안정 한계선(Knee of the Curve)과 붕괴 조건을 탐색했습니다. 추가로 **스레드 구성 변경**과 **방 구성 변경(1000방×10명)** 의 효과를 데이터로 검증했습니다.

### 테스트 환경

| 구분 | 사양 |
| --- | --- |
| **Server** | AWS EC2 c6i.4xlarge (16 vCPU, 32GB RAM), Windows Server 2022 |
| **Client** | AWS EC2 c6i.large (2 vCPU, 4GB RAM) × 4대 (각 2,500봇 = 총 10,000봇) |
| **Network** | 동일 리전, 동일 클러스터 배치 그룹 |
| **채팅 시나리오** | 로그인 → 방 입장 → **연속 채팅** (퇴장/재입장 없음, 순수 브로드캐스트 부하 측정) |
| **아키텍처** | v3 LockFree — CAS 기반 MPSC Queue + Lock-Free Object Pool + Strand 패턴 |

### 500방 × 20명 — 채팅 빈도별 한계 탐색

#### 스레드 구성 영향 분석 (chatMin=300ms, chatMax=1000ms)

동일 부하 조건에서 Logic Thread와 IO Worker 비율을 변경하며 효과를 측정했습니다.

| Logic | IO | recv_pkt/s | lat_avg | lat_p99 | CPU | 메모리 | 비고 |
|:-----:|:--:|:---------:|:------:|:------:|:---:|:-----:|------|
| 4 | 8 | ~284K/s | ~25ms | ~34ms | 42% | 396MB | 기준선 |
| 8 | 8 | ~284K/s | ~24ms | ~34ms | 65% | 396MB | CPU +23%p, latency 변화 미미 |
| 8 | 16 | ~284K/s | ~25ms | ~34ms | 67% | 396MB | IO 2배 증가 효과 없음 |

> **Logic Thread 4→8 시 CPU +23%p 상승, latency 변화 없음** — avg 650ms 채팅 간격은 부하가 낮아 Logic Thread 4개로도 충분히 소화 가능한 수준입니다. 스레드를 늘려도 처리할 일이 늘어나지 않으므로 latency 개선이 없습니다.
>
> **IO Worker 8→16 증가 시 CPU가 +2%p만 상승** — 추가 IO Thread가 거의 유휴 상태였음을 의미합니다. IOCP 완료 통지 처리보다 Logic Thread의 메시지 처리(브로드캐스트)가 훨씬 무거운 작업이기 때문입니다.

#### 채팅 간격별 한계 탐색 (Logic=8, IO=16)

chatMin/chatMax를 점진적으로 줄여가며 서버 한계를 탐색했습니다.

| chatMin/Max | avg 간격 | recv_pkt/s | lat_avg | lat_p99 | CPU | JobPool | 상태 |
|:----------:|:-------:|:---------:|:------:|:------:|:---:|:------:|:----:|
| 300/1000 | 650ms | ~284K/s | ~24ms | ~34ms | 67% | 정상 | ✅ 안정 |
| 200/600 | 400ms | ~379K/s | ~28ms | ~38ms | 75% | 정상 | ✅ 안정 한계 |
| 150/450 | 300ms | 불안정 | 31ms→2,600ms | →4,900ms | 80% | 1회 오류 | 🔴 Death Spiral |
| 100/300 | 200ms | 불안정 | 3,000ms→14,000ms | →14,000ms | 80%+ | 지속 소진 | 🔴 완전 붕괴 |

> **안정 한계**: chatMin=200, chatMax=600 (평균 간격 400ms), recv_pkt/s ~379K/s, lat_avg ~28ms.
> **붕괴 시작**: chatMin=150, chatMax=450 이하에서 JobPool(200,000) 소진이 발생하며 latency가 수렴하지 않고 지속 증가하는 Death Spiral 패턴이 나타남.

#### Logic Thread 증가로 붕괴 역전 (chatMin=100, chatMax=300)

Logic=8에서 완전 붕괴했던 조건(chatMin=100/300)을 **Logic=16, IO=8** 로 변경하여 재측정했습니다.

| Logic | IO | recv_pkt/s | lat_avg | lat_p99 | CPU | JobPool | 상태 |
|:-----:|:--:|:---------:|:------:|:------:|:---:|:------:|:----:|
| 8 | 16 | 붕괴 | 3s→14s | →14s | 80%+ | 🚨 소진 | 🔴 Death Spiral |
| **16** | **8** | **~539K/s** | **~32ms** | **~83ms** | **95%** | **정상** | **✅ 안정** |

> Logic Thread를 2배로 늘리고 IO Worker를 절반으로 줄인 결과, **동일 조건에서 Death Spiral이 완전히 해소**되었습니다.
> 이는 붕괴의 원인이 IO Worker 부족이 아닌 **Logic Thread의 메시지 처리 속도 부족**이었음을 데이터로 증명합니다.
> CPU 95%를 사용하는 대신 lat_avg 32ms로 안정 동작하며, lat_p99 83ms는 CPU 여유 부족으로 인한 간헐적 지연입니다.

### 1000방 × 10명 — 브로드캐스트 비용 비교

동일한 총 인원(10,000명)을 유지하되 방 구성을 변경(방 수 2배, 방당 인원 절반)하여 **브로드캐스트 비용이 서버 한계에 미치는 영향**을 측정했습니다.

| chatMin/Max | avg 간격 | recv_pkt/s | lat_avg | lat_p99 | CPU | 메모리 | JobPool | 상태 |
|:----------:|:-------:|:---------:|:------:|:------:|:---:|:-----:|:------:|:----:|
| 300/1000 | 650ms | ~164K/s | ~15.5ms | ~24ms | 69% | 370MB | 정상 | ✅ 여유 |
| 100/300 | 200ms | ~390K/s | ~28ms | ~41ms | 78% | 397MB | 정상 | ✅ 안정 |
| 50/150 | 100ms | ~320K/s | ~3,200ms | ~6,600ms | 82% | 419MB | 1회 오류 | ⚠️ 고지연 |

> **핵심 비교 — 동일 chatMin=100/300 조건:**
> - 500방×20명 (Logic=8): Death Spiral (lat_avg → 14초) 🔴
> - **1000방×10명 (Logic=8): lat_avg ~28ms 안정** ✅
>
> 같은 총 인원이라도 방당 인원이 절반(20명→10명)이면 **브로드캐스트 횟수가 절반(19회→9회)** 으로 줄어들어,
> 서버의 안정 운영 한계가 약 **2배 높은 채팅 빈도**까지 확장됩니다.

### 핵심 발견 요약

#### 1. 서버 안정 한계선 — recv_pkt/s ~380K에서 수렴

두 방 구성 모두 **lat_avg ~28ms, recv_pkt/s ~380~390K** 부근에서 안정 한계에 도달했습니다.

| 시나리오 | 안정 한계 조건 | recv_pkt/s | lat_avg |
|---------|-------------|:---------:|:------:|
| 500방×20명 (L8/IO16) | chatMin=200, chatMax=600 | ~379K/s | ~28ms |
| 1000방×10명 (L8/IO16) | chatMin=100, chatMax=300 | ~390K/s | ~28ms |

스레드 구성(Logic=16, IO=8)을 변경하면 ~539K/s까지 확장 가능합니다.

#### 2. 병목은 Logic Thread — IO Worker가 아닌 Logic 처리 속도

| 변경 | CPU 변화 | recv_pkt/s 변화 | 의미 |
|------|:-------:|:-----------:|------|
| IO Worker 8→16 | +2%p | 변화 없음 | IO Thread 추가는 효과 없음 |
| **Logic 8→16 (IO 16→8)** | **+28%p** | **붕괴 → 539K/s 안정** | **Logic이 진짜 병목** |

#### 3. 방당 인원 수 = 서버 한계를 결정하는 핵심 변수

| 구성 | 브로드캐스트/채팅 | 안정 한계 간격 | 배수 |
|------|:-------------:|:----------:|:---:|
| 500방×20명 | 19회 | avg 400ms | 1× |
| 1000방×10명 | 9회 | avg 200ms | 2× |

**동시 접속자 수보다 "방당 브로드캐스트 비용"이 서버 설계의 핵심 파라미터**임을 확인했습니다.

#### 4. 채팅 빈도별 스레드 구성 권장값

| 시나리오 | 채팅 빈도 (chatMin~Max) | 권장 Logic | 권장 IO | 비고 |
|---------|:--------------------:|:---------:|:------:|------|
| 500방×20명 (여유) | 300~1000ms (avg 650ms) | 8 | 16 | CPU 67%, 여유 있음 |
| 500방×20명 (안정 한계) | 200~600ms (avg 400ms) | **16** | **8** | CPU 95%, 안정 한계 |
| 1000방×10명 (안정 한계) | 100~300ms (avg 200ms) | 8 | 16 | CPU 78%, 여유 있음 |

> 채팅 빈도가 증가할수록 Logic Thread가 더 많은 CPU를 필요로 합니다. **Logic Thread ≫ IO Worker** 구성이 고부하 브로드캐스트 환경에서 효율적입니다.

#### 5. 풀(Pool) 안정성 — 전 테스트 구간 검증 완료

| Pool | 전 테스트 구간 누적 | 비고 |
|------|:------------------:|------|
| **SendPool Alloc Fail** | **0회** | 모든 부하에서 안정 |
| **JobPool Alloc Fail** | 과부하 시에만 발생 | 처리 속도 < 생성 속도일 때, 풀 크기 증가는 근본 해결 아님 |

---

## Scenario B: MutexCV vs LockFree 아키텍처 비교 (16-core c6i.4xlarge)

v2 MutexCV와 v3 LockFree 아키텍처를 동일한 서버·클라이언트 환경에서 비교했습니다.

### MutexCV 측정 결과

> 아키텍처: v2 MutexCV — `GlobalQueue_MutexCV` (`std::mutex + std::condition_variable`)
> 클라이언트: c6i.xlarge × 4대, 채팅 간격 3~5초(평균 4초), 퇴장/재입장 반복

#### 방 구성별 측정치 (스레드 고정: Worker=8, IOWorker=8, Logic=4)

| 방 구성 | CPU | lat_avg | lat_p99 | 상태 |
| --- | --- | --- | --- | --- |
| 200방 × 50명 | 50% | 20.73 ms | ~32ms (최대 1,310ms) | ✅ 안정 |
| 500방 × 20명 | 90% | 49.24 ms | ~156ms (최대 344ms) | ⚠️ 포화 구간 |
| 1000방 × 10명 | 100% | 11,435 ms | 36,066 ms | 🔴 Logic Thread 부족 붕괴 |

> Logic Thread 4개로 1,000개의 방 큐를 처리하는 구조에서 **Strand 스케줄링 오버헤드**가 병목으로 작용했습니다.

#### 시나리오별 권장 스레드 구성 (16 vCPU 기준, MutexCV 기준 도출)

방 인원이 많으면 I/O(WSASend fan-out)가 병목이 되고, 방 수가 많으면 Logic(Strand 스케줄링)이 병목이 됩니다.

| 시나리오 | 병목 | MaxIOWorkerThread | MaxLogicThread |
| --- | --- | :-: | :-: |
| 10방 × 1000명 | IOCP Worker (fan-out 999×) | **12** | **2** |
| 50방 × 200명 | IOCP Worker (fan-out 199×) | **10** | **4** |
| 200방 × 50명 | 균형 | **8** | **6** |
| 500방 × 20명 | Logic Thread (500개 방 큐) | **6** | **8** |
| 1000방 × 10명 | Logic Thread (1000개 방 큐) | **4** | **10** |

> 원칙: IO + Logic ≤ 14 (16 vCPU에서 OS 여유 자원 2개 제외). 방 인원이 많으면 IO에, 방 수가 많으면 Logic에 코어를 집중합니다.

### LockFree vs MutexCV 아키텍처 비교 (500방 × 20명, 동일 서버 환경)

| 지표 | MutexCV | LockFree | 비고 |
|------|:-------:|:--------:|------|
| **채팅 간격** | avg 4초 | avg 650ms | LockFree **6배 높은 빈도** |
| **스레드** | W8/IO8/L4 | L8/IO16 | — |
| **lat_avg** | 49.24ms | ~24ms | LockFree **51% 개선** |
| **lat_p99** | ~156ms | ~34ms | LockFree **78% 개선** |
| **CPU** | 90% | 67% | LockFree **23%p 여유** |

> LockFree 아키텍처는 **6배 높은 채팅 빈도**에서도 MutexCV 대비 latency 51% 개선, CPU 23%p 절감을 달성했습니다.
> Lock 경합 제거(MPSC Queue)와 Strand 패턴의 효율적 직렬화가 결합된 결과입니다.
> 아키텍처 전환 전 과정은 [서버 아키텍처 진화 문서](서버%20아키텍처%20진화%20및%20성능%20벤치마크.md)를 참조해 주십시오.
